# Dockerized OpenClaw Plugin/Skill Directory Information

## Plugin Management in Dockerized Instances

**Date:** 2026-05-07T19:26:00Z (Heartbeat-Integrated L0H)

**Observation:** The Dockerized OpenClaw instance either (a) doesn’t use a traditional `/data/.openclaw/plugins` directory, or (b) plugins are managed via its `skills` or `extensions` directory. 

**Context:** This information is critical for understanding how OpenClaw manages its extensions and capabilities when running within a Docker container, especially during migration efforts or troubleshooting.
