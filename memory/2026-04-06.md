
## Heartbeat signals
- [lesson] `message` tool with `channel='whatsapp'` and phone number as `target` is currently unsupported or misconfigured. Direct external messaging is unreliable for heartbeat flags.
- [workflow] Switched to logging critical heartbeat flags directly to `memory/YYYY-MM-DD.md` as an internal fallback when external messaging fails.
- [project] (career-transition) Gmail OAuth token revoked, blocking job email monitoring. IMAP migration to himalaya needed by April 7 for Batch 2.
- [project] (system-rebuild) User proposed a complete system rebuild, including potential hardware actions, due to workflow frustration.
